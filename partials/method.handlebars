<div id="method_{{name}}"
     class="bs-callout bs-callout-info method item{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}}{{#if extended_from}} inherited{{/if}}">


{{#is_constructor}}
	<h4>Constructor</h4>
{{/is_constructor}}

{{#if final}}
	<span class="label label-danger final">final</span>
{{/if}}

{{#if access}}
	<span class="label label-primary {{access}}">{{access}}</span>
{{else}}
	<span class="label label-primary public">public</span>
{{/if}}

{{#if static}}
	<span class="label label-default static">static</span>
{{/if}}

<span>{{name}}</span>

{{#if params}}

	<span class="paren">(</span>
	<ul class="inline commas">
		{{#params}}
			<li class="arg">
				{{#if optional}}
					<code class="optional">[{{name}}{{#if optdefault}}={{optdefault}}{{/if}}]</code>
				{{else}}
					<code>{{name}} : {{#crossLink type}}{{/crossLink}}</code>
				{{/if}}
			</li>
		{{/params}}
	</ul>
	<span class="paren">)</span>
{{else}}
	<span class="paren">()</span>
{{/if}}

{{#if return}}
	<span class="returns-inline">
	            : <span class="type">{{#crossLink returnType}}{{/crossLink}}</span>
	        </span>
{{/if}}


{{#if deprecated}}
	<span class="flag deprecated"{{#if deprecationMessage}} title="{{deprecationMessage}}"{{/if}}>deprecated</span>
{{/if}}

{{#if chainable}}
	<span class="flag chainable">chainable</span>
{{/if}}

{{#if async}}
	<span class="flag async">async</span>
{{/if}}

<div class="meta">
	{{#if overwritten_from}}
	<p>Inherited from
		<a href="{{crossLinkRaw overwritten_from/class}}#method_{{overwritten_from/name}}">
			{{overwritten_from/class}}
		</a>
		{{#if foundAt}}
			but overwritten in
		{{/if}}
	{{else}}
		{{#if extended_from}}
		<p>Inherited from
			<a href="{{crossLinkRaw extended_from}}#method_{{name}}">{{extended_from}}</a>:
		{{else}}
			{{#providedBy}}
				<p>Provided by the <a href="../modules/{{.}}.html">{{.}}</a> module.</p>
			{{/providedBy}}
		<p>

		{{/if}}
	{{/if}}
</p>



	{{#if deprecationMessage}}
		<p>Deprecated: {{deprecationMessage}}</p>
	{{/if}}

	{{#if since}}
		<p>Available since {{since}}</p>
	{{/if}}
</div>

<div class="meta code_at">
	{{#if foundAt}}
		<a class="btn btn-default btn-xs" href="{{foundAt}}">view code</a>
	{{/if}}
</div>

<p>
	{{{methodDescription}}}
</p>


<div class="panel panel-default">
	{{#if params}}

		<div class="panel-heading">Parameters:</div>

		<table class="table">
			<thead>
			<tr>
				<th>Name:</th>
				<th>Type:</th>
				<th>Description:</th>
			</tr>
			</thead>
			<tbody>
			{{#params}}
				<tr>
					<td>
						{{#if optional}}
							<code class="param-name optional">[{{name}}{{#if optdefault}}={{optdefault}}{{/if}}]</code>

							<span class="flag optional" title="This parameter is optional.">optional</span>
						{{else}}
							<code class="param-name">{{name}}</code>
						{{/if}}
					</td>
					<td>
						<span class="type">{{#crossLink type}}{{/crossLink}}</span>
					</td>
					<td>
						{{{description}}}
					</td>
				</tr>
			{{/params}}
			</tbody>
		</table>


		<!--
	<div class="params">
		<h4>Parameters:</h4>

		<ul class="params-list">
			{{#params}}
				<li class="param">
					{{#if optional}}
						<code class="param-name optional">[{{name}}{{#if optdefault}}={{optdefault}}{{/if}}]</code>
						<span class="type">{{#crossLink type}}{{/crossLink}}</span>
						<span class="flag optional" title="This parameter is optional.">optional</span>
					{{else}}
						<code class="param-name">{{name}}</code>
						<span class="type">{{#crossLink type}}{{/crossLink}}</span>
					{{/if}}

			{{#if multiple}}
						<span class="flag multiple"
						      title="This argument may occur one or more times.">multiple</span>
					{{/if}}

					<div class="param-description">
						{{{description}}}
					</div>

					{{#if props}}
						<ul class="params-list">
							{{#props}}
								<li class="param">
									{{#if optional}}
										<code class="param-name optional">[{{name}}{{#if optdefault}}
											={{optdefault}}{{/if}}]</code>
										<span class="type">{{#crossLink type}}{{/crossLink}}</span>
											<span class="flag optional"
											      title="This parameter is optional.">optional</span>
									{{else}}
										<code class="param-name">{{name}}</code>
										<span class="type">{{#crossLink type}}{{/crossLink}}</span>
									{{/if}}

									<div class="param-description">
										{{{description}}}
									</div>

									{{#if props}}
										<ul class="params-list">
											{{#props}}
												<li class="param">
													{{#if optional}}
														<code
															class="param-name optional">[{{name}}{{#if optdefault}}
															={{optdefault}}{{/if}}]</code>
														<span class="type">{{#crossLink type}}{{/crossLink}}</span>
															<span class="flag optional"
															      title="This parameter is optional.">optional</span>
													{{else}}
														<code class="param-name">{{name}}</code>
														<span class="type">{{#crossLink type}}{{/crossLink}}</span>
													{{/if}}

													<div class="param-description">
														{{{description}}}
													</div>
												</li>
											{{/props}}
										</ul>
									{{/if}}
								</li>
							{{/props}}
						</ul>
					{{/if}}
				</li>
			{{/params}}
		</ul>
	</div>
	-->
	{{/if}}

	<div class="panel-body">
		Returns:
		{{#if return}}
			{{#return}}
				{{#if description}}
					{{#if type}}
						<span class="type">{{#crossLink type}}{{/crossLink}}</span>
					{{/if}}
					{{{description}}}
				{{else}}
					{{#if type}}
						<span class="type">{{#crossLink type}}{{/crossLink}}</span>
					{{/if}}
				{{/if}}
			{{/return}}
		{{else}}
			<span class="type">undefined</span>
		{{/if}}
	</div>

	{{#example}}
		<div class="panel-body">
			<h4>Example:</h4>

			<div class="example-content">
				{{{.}}}
			</div>
		</div>
	{{/example}}
</div>
</div>